import React from 'react';
import TextEditor from '../TextEditor/TextEditor';

const Header = () => {
    return (
        <div>
           <TextEditor/>
        </div>
    );
};

export default Header;